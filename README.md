# Crypto Dashboard

Интерактивное приложение для мониторинга криптовалют с данными CoinGecko. Обновление рынка каждые 30 секунд, избранное, поиск, сортировка, адаптивная таблица, light/dark тема, детальная страница актива и offline-состояние.

## ✨ Возможности

* Реальное обновление рынка каждые 30 секунд
* Адаптивная таблица (desktop — таблица, mobile — карточки)
* Поиск по рынку
* Сортировка над таблицей (desktop)
  — По названию
  — По цене
* Избранное (звёзды)
  — Хранится в Pinia
  — Синхронизируется с localStorage
* Dark / Light Theme (автосохранение)
* Offline-режим
  — баннер «нет сети»
  — данные остаются доступными
* Скелетон loader + error-state
* График цены актива (7 дней)
* Полностью модульная архитектура FSD

## 🛠️ Технологии

* Vue 3 + TypeScript
* Pinia (state management)
* Vite
* Vue Router
* ApexCharts (графики)
* FSD — Feature-Sliced Design
* CSS Variables + адаптивная система цветов
* localStorage
* CoinGecko API

## 🚀 Запуск проекта

```bash
npm install
npm run dev
```

Продакшен:

```bash
npm run build
npm run preview
```

## 📁 Архитектура (FSD)

Проект структурирован по методологии Feature-Sliced Design (FSD).

```
src/
 ├── app/                         # Инициализация приложения
 │   ├── providers/               # Провайдеры (router и т.п.)
 │   └── App.vue                  # Корневой layout, тема, header
 │
 ├── entities/                    # Бизнес-сущности
 │   └── crypto/
 │       ├── api/                 # crypto.api.ts — работа с CoinGecko
 │       ├── model/               # crypto.store.ts, типы, доменная логика
 │       └── index.ts             # публичный интерфейс сущности
 │
 ├── features/                    # Фичи (узкие пользовательские сценарии)
 │   └── crypto-filters/
 │       ├── ui/                  # CryptoFilters.vue — поиск + mobile-сортировка
 │       └── index.ts
 │
 ├── pages/                       # Страницы (route-level)
 │   ├── asset/                   # Страница конкретного актива
 │   │   ├── ui/                  # AssetPage.vue
 │   │   └── index.ts
 │   ├── dashboard/               # Главная страница дашборда
 │   │   ├── ui/                  # DashboardPage.vue
 │   │   └── index.ts
 │   └── not-found/               # 404
 │       ├── ui/                  # NotFoundPage.vue
 │       └── index.ts
 │
 ├── shared/                      # Общие модули, не завязанные на конкретную фичу
 │   ├── api/
 │   │   └── httpClient.ts        # Базовый HTTP-клиент (axios)
 │   ├── stores/
 │   │   └── app.store.ts         # useAppStore — онлайн/офлайн и прочие глобальные флаги
 │   └── styles/
 │       └── reset.css            # reset-стили (обнуление браузерных отступов и т.д.)
 │
 ├── widgets/                     # Крупные UI-блоки, собирающие entities + features
 │   ├── market-table/            # Таблица рынка
 │   │   └── ui/                  # MarketTable.vue — таблица + mobile-grid + skeleton/error
 │   └── price-chart/             # График цены на странице актива
 │       └── ui/                  # PriceChart.vue (график за 7 дней на ApexCharts)
 │
 ├── main.ts                      # Точка входа Vite: создание app, Pinia, router, init стора
 └── style.css                    # Глобальные стили и CSS-переменные темы

```

## 🧩 Основная логика

### 🔹 1. State (Pinia)

**useCryptoStore**

* хранит items, isLoading, error
* автообновление каждые 30 секунд
* favorites (localStorage)

**useAppStore**

* isOnline
* слушатели online/offline

### 🔹 2. Dashboard

* загрузка при первом рендере
* автообновление → cryptoStore.loadMarkets()
* фильтры (search, sortBy, sortDir)
* сортировка над таблицей (desktop)
* offline-баннер

### 🔹 3. MarketTable (виджет)

* desktop: таблица
* mobile: grid-карточки
* skeleton loader
* error-state
* избранное
* click → переход к AssetPage

### 🔹 4. Theme

* хранится в localStorage
* переключение кнопкой в шапке
* на уровне html data-theme="dark"

### 🔹 5. Offline Mode

* слушаем события браузера
* показываем баннер, если navigator.onLine = false
* таблица остаётся доступна (кэш Pinia)

### 🔹 6. API

CoinGecko:

* /coins/markets
* /coins/{id}/market_chart

Все запросы — через entities/crypto/api.

## ✔️ Дополнительно

### 📡 Автообновление рынка

В useCryptoStore.startAutoRefresh():

```ts
setInterval(() => {
  console.log("Auto-refresh:", new Date().toLocaleTimeString());
  this.loadMarkets();
}, 30_000);
```

Не зависит от переходов страниц.
